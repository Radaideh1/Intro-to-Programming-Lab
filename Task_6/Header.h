#include <iostream>
#include <cmath>
using namespace std;


/*This function takes a character value and return true if a
given character is small adn otherwise returns false.*/
bool IsSmall(char ch);
/*This function takes a variable x and returns f(x).*/
void CalculateFunction(double start, double step , double end);
/*User Enter 3 number and this function will return teh minimum number.*/
float MinNumber(float first);
/*This make to help CalaEquation!!*/
float CalcuFor(int T);
/*Tis function calculates and returns teh value of F*/
float CalaEquation(int T);
